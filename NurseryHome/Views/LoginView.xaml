<Window x:Class="NurseryHome.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:NurseryHome.Views"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Autentificare" Height="250" Width="350">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Margin="0,10" Text="{Binding Email}" />
        <PasswordBox Margin="0,10" Grid.Row="1" Name="PasswordBox"/>
        <Button Grid.Row="2" Margin="0,20" Height="30"
                Content="Login"
                Command="{Binding LoginCommand}" />
    </Grid>
</Window>


